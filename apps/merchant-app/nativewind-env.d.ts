// Committed type references. Expo also writes expo-env.d.ts on start, but that file is ignored by
// git, so a fresh clone typechecks from this one.
/// <reference types="expo/types" />
/// <reference types="nativewind/types" />
