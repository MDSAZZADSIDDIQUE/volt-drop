/** The only locale in Phase 1 (spec §3: GBP, en-GB, Europe/London). */
export const LOCALE = 'en-GB';

/** Business rules and displayed times use UK time, whatever the device is set to (spec §3). */
export const TIME_ZONE = 'Europe/London';

export { messages, type Messages } from './messages/en-GB.js';
